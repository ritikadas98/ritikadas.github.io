node_modules/
# Screenshot intermediates, one directory per deck or per width. These are
# rebuilt by shoot.mjs whenever a deck changes, so none of them belongs in git.
# Globbed rather than listed: the list missed out-crew/ the first time a new
# deck was exported.
out/
out-*/
*.pdf
