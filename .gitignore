# Private working documents — not for public view on GitHub
BUILD_SPEC.md
PORTFOLIO_WORKLOG.md

# Reusable migration brief for the GCP projects. Nothing to do with this site —
# it lives here only because it was written here, and gets copied out.
VERTEX_MIGRATION_PROMPT.md

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Article drafts — source text and full-res screenshots. Never published; the
# built pages and their web-sized assets are what get committed.
writing-drafts/

# Full-res captures dropped straight into an article folder. A phone screenshot
# is ~2MB of PNG; only the cropped, web-sized JPEG beside it gets published.
writing/**/*.png
writing/**/Simulator Screenshot*

# Downloaded webfonts and scratch HTML for tools/og-card.py. Regenerated on
# demand; the rendered cards in assets/og/ are what get committed.
tools/.fontcache/

# Resume working copy. Carries a phone number and a personal email, so it stays
# out of the public repo — the published version is a PDF we add deliberately.
Ritika_Das_Resume.md
Ritika_Das_Resume.pdf

# Local editor/agent settings, not part of the site.
.claude/

# Scratch output from local page-audit scripts. Not part of the site.
result.txt

# PowerPoint lock files, written while a deck is open
~$*
